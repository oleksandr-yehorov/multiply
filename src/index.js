module.exports = function multiply(first, second) {
  // your solution

  let one = first.split('').reverse();  // split and reverse first and second numbers
  let two = second.split('').reverse(); // into two arrays

  let temp = [];                        // initializing temporary array for multiplying
                                        // each number in arrays

  
}
